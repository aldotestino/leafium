# Leafium Smart Contract

1. Run local blockchain node with:
    ```sh
    yarn run-node
    ```
2. Deploy smart contract with:
    ```sh
    yarn deploy:localhost
    ```
3. Run unit test with:
    ```sh
    yarn test:unit
    ```
